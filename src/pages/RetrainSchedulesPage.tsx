/**
 * Body of the app's Retrain schedules config page. Grafana renders it for
 * /plugins/<app plugin id>?page=schedules (see addConfigPage in src/module.tsx).
 * It takes no props: unlike the Configuration page it persists no plugin settings.
 */
export { default } from '../components/AppConfig/RetrainSchedules';
